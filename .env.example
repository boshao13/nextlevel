# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=nextlevel_crm

# Auth
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2b$10$... (generate with: node -e "require('bcryptjs').hash('yourpassword',10).then(h=>console.log(h))")
JWT_SECRET=change-this-to-a-random-string

# Manager
MANAGER_USERNAME=jesusg
MANAGER_PASSWORD_HASH=$2b$10$... (generate with: node -e "require('bcryptjs').hash('yourpassword',10).then(h=>console.log(h))")

# Payroll / Approver
PAYROLL_USERNAME=aimeeg
PAYROLL_PASSWORD_HASH=$2b$10$... (generate same way as above)

# Resend (transactional email)
RESEND_API_KEY=re_xxxxxxxxxxxx
LEAD_FROM_EMAIL=leads@your-verified-domain.com
LEAD_TO_EMAIL=where-leads-should-go@example.com
